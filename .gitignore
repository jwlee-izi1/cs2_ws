# ROS2 build artifacts
build/
install/
log/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv*/
venv*/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# External ROS2 dependencies (fetched via deps.repos)
src/crazyswarm2/
src/crazyflie-simulation/

# External dependency clones (forks) — local edits preserved in vendor/DEPENDENCIES.md
# Anchored to repo root so they do NOT match vendor/crazyflie-firmware/ etc.
/crazyflie-firmware/
/cflib-src/
/.crazyswarm2-upstream-backup/

# Experiment data & results (large/regenerable; added back selectively later)
/exp1/
/exp2/
/fed_dcsa_logs/
/cache/
/drone_exp1*.csv
*.mcap
*.npz

# Documents & installers (no PDFs per upload policy)
*.pdf
*.deb

# Stray runtime artifacts
/.gui_camera_pose.txt
