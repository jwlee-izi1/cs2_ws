"""Pure-Python RL training for the federated coverage policy.

Sibling to the existing rl_demo ROS playback layer. Independent Python module
(no ROS dependency at training time). Imports the existing M4 fire model and
FedDCSA optimizer for numerical fidelity with the live system.
"""
